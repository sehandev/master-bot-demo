import React from "react";

import { Container, Sidebar } from "@components";

const Home: React.FC = () => {
    return (
        <Container>
            <Sidebar model_name="mbert">
            </Sidebar>
        </Container>
    );
};

export default Home;
